Problem 2 

support (let (variable_name number) item_1 item_2 ... )) and it will print the return value of the last item.
(let (a 5)(+ a 5)(* a 5)) returns 25

copy the code from lis.py into mini-lisp.py and did some modify.

support function such like (car '( 1 2 3))
